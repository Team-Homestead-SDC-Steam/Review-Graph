# steam-reviews-graph

Configuration notes:

Requires MySQL 8.x

Requires a /db/config.js file containing:
module.exports = {
  host: 'localhost',
  user: 'root',
  password: '{your password}',
  database: 'steam',
};

